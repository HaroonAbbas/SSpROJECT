/*
Deployment script for SSDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "SSDB"
:setvar DefaultFilePrefix "SSDB"
:setvar DefaultDataPath "C:\Users\DELL\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\DELL\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column SIDE_TYPE on table [dbo].[SS_Sides] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[SS_Sides])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Starting rebuilding table [dbo].[SS_Sides]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_SS_Sides] (
    [SIDE_NO]     INT            NOT NULL,
    [SIDE_TYPE]   INT            NOT NULL,
    [SIDE_A_NAME] NVARCHAR (MAX) NULL,
    [SIDE_E_NAME] NVARCHAR (MAX) NULL,
    CONSTRAINT [tmp_ms_xx_constraint_PK_SS_Sides1] PRIMARY KEY CLUSTERED ([SIDE_NO] ASC, [SIDE_TYPE] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[SS_Sides])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_SS_Sides] ([SIDE_NO], [SIDE_TYPE], [SIDE_A_NAME], [SIDE_E_NAME])
        SELECT   [SIDE_NO],
                 [SIDE_TYPE],
                 [SIDE_A_NAME],
                 [SIDE_E_NAME]
        FROM     [dbo].[SS_Sides]
        ORDER BY [SIDE_NO] ASC, [SIDE_TYPE] ASC;
    END

DROP TABLE [dbo].[SS_Sides];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_SS_Sides]', N'SS_Sides';

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_constraint_PK_SS_Sides1]', N'PK_SS_Sides', N'OBJECT';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Update complete.';


GO
